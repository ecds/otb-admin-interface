{{#if session.isAuthenticated}}
 <UkButton @on-click={{action "logOut"}}>Logout</UkButton>
{{else}}
<UkModal @modalName="ecds-login" as |modal|>
  <modal.open>
    Login
  </modal.open>
  <modal.dialog>
    <modal.body>
      <LoginForm />
    </modal.body>
  </modal.dialog>
</UkModal>
{{/if}}
