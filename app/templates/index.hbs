{{!-- <nav class="uk-navbar uk-light uk-background-secondary uk-margin" uk-navbar>

  <div class="uk-navbar-left">
    <span class="uk-logo">
      <img src="/assets/images/otblogo.png" alt="Branding logo">
      {{sortedTours.firstObject.tenant_title}}
    </span>
  </div>
</nav>
{{#if (media "isDesktop")}}
  {{#if (eq sortedTours.length 1)}}
    {{#each sortedTours as |tour|}}
      <div style={{tour.splashBackground}} class="tour-list-single">
        <LinkTo @route="tour" @model={{tour.slug}}>
          <div class="uk-overlay uk-overlay-primary">
            <div class="uk-position-center">
              <h1 class="uk-heading-primary uk-text-center">{{tour.title}}</h1>
              <p class="uk-text-meta uk-text-center">{{tour.stop_count}} Stops</p>
              <UkButton @color="secondary" @class="uk-align-center uk-width-1-2">start tour</UkButton>
            </div>
          </div>
        </LinkTo>
      </div>
    {{/each}}
  {{else if (gt sortedTours.length 3)}}
    <div class="uk-child-width-1-3 uk-grid-small" uk-grid masonry="true">
      {{#each sortedTours as |tour|}}
        <div style={{tour.splashBackground}} class="tour-list-single">
          <LinkTo @route="tour" @model={{tour.slug}}>
            <div class="uk-overlay-primary uk-position-cover">
              <div class="uk-position-center">
                <h1 class="uk-heading-primary uk-text-center">{{tour.title}}</h1>
                <p class="uk-text-meta uk-text-center">{{tour.stop_count}}Stops</p>
                <UkButton @color="secondary" @class="uk-align-center uk-width-1-2">start tour</UkButton>
              </div>
            </div>
          </LinkTo>
        </div>
      {{/each}}
    </div>
  {{else}}
    <div class="uk-child-width-expand uk-grid-small" uk-height-match uk-grid>
      {{#each sortedTours as |tour|}}
        <div style={{tour.splashBackground}} class="tour-list-single">
          <div class="uk-cover-container uk-transition-toggle" style="height: 100vh;">
            <LinkTo @route="tour" @model={{tour.slug}}>
              <img src={{concat baseUrl tour.splash.original_image.url}} alt={{concat "Cover image for the " tour.title " tour. The image is titled " tour.splash.title " and captioned: " tour.splash.caption}} class="otb-cover-image uk-transition-scale-up uk-transition-opaque">
                <div class="uk-overlay-primary uk-position-cover">
                  <div class="uk-position-center uk-width-3-4">
                  <h1 class="uk-heading-primary uk-text-center">{{tour.title}}</h1>
                  <p class="uk-text-meta uk-text-center">{{tour.stop_count}} Stops</p>
                  <UkButton @color="secondary" @class="uk-align-center uk-width-1-2">start tour</UkButton>
                </div>
              </div>
            </LinkTo>
          </div>
        </div>
      {{/each}}
    </div>
  {{/if}}
{{else}}
  {{#each sortedTours as |tour|}}
    <LinkTo @route="tour" @model={{tour.slug}}>
      <div class="uk-card uk-card-default uk-card-small {{if tour.show "uk-animation-slide-bottom"}}">
        <div class="uk-card-media-top">
          <img src="{{baseUrl}}{{tour.splash.original_image.tablet.url}}" alt="" style="width: 100vw;">
        </div>
        <div class="uk-card-body">
          <h4 class="uk-card-title">{{tour.title}}</h4>
          {{tour.stops.length}} Stops
        </div>
      </div>
    </LinkTo>
  {{/each}}
{{/if}} --}}